import 'package:flutter/material.dart';

class SelectPacient extends StatelessWidget {
  const SelectPacient({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
