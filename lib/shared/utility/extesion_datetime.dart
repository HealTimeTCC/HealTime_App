import 'package:flutter/material.dart';

extension DateTimeExtension on DateTime {
  DateTime  formatDateTimeOfDay({required DateTime date, required TimeOfDay time}) {
    return DateTime.now();
  }
}